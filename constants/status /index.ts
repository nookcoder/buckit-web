export enum ProjectStatus {
  Any = 'any',
  Before = 'before',
  FUNDING_PROGRESS = 'progress',
  FundingEnd = 'end',
  Opening = 'opening',
}

export enum OrderBy {
  CREATED_AT,
  DEAD_LINE,
  LIKE,
}
